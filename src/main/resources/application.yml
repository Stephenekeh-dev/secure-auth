spring:
  application:
    name: secure-auth

  datasource:
    url: jdbc:postgresql://localhost:5432/secure_auth
    username: postgres
    password: 1234567Steven..,
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect

  redis:
    host: localhost
    port: 6379

  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true

  mail:
    host: smtp.gmail.com
    port: 465                                 # ✅ use 465 for SSL
    username: ekehsteven2@gmail.com
    password: gzbs oooi akch phbv               # ✅ env variable
    properties:
      mail:
        smtp:
          auth: true
          ssl:
            enable: true                      # ✅ correct for port 465
          starttls:
            enable: false                     # ✅ disable — conflicts with SSL on 465

server:
  port: 8080

jwt:
  secret: "v2DQ/qdsoALW2JjL8a86ENcoBIE1QWAm2sIn10LJa1U="                      # ✅ env variable
  expiration-ms: 3600000

app:
  base-url: ${APP_BASE_URL:http://localhost:8080}  # ✅ used by EmailService

file:
  upload-dir: ./uploads/profile-pictures

# ✅ Reduce debug noise in production — consider 'INFO' when deploying
logging:
  level:
    root: INFO
    org.springframework.web: DEBUG
    org.hibernate.SQL: DEBUG
    com.steve.secure_auth: DEBUG